{
	"name": "irso-projeto",
	"version": "1.0.0",
	"scripts": {
		"format": "npx prettier --write \"**/*.php\"",
		"dev": "docker-compose -f compose.yaml up -d --build",
		"down": "docker-compose -f compose.yaml down"
	},
	"devDependencies": {
		"@prettier/plugin-php": "^0.24.0",
		"prettier": "^3.7.4"
	}
}
